{
  "name": "whitelistthem",
  "version": "2.0.0",
  "description": "Chrome Extension that filters through allowed channels and allows the user to curate their own experience",
  "main": "index.js",
  "scripts": {
		"build": "npx tsc && npx sass ./src/styles/ytstyles.scss dist/styles.css && npx sass ./src/styles/tagger.scss dist/tagger.css",
		"watch-ts": "npx tsc -w",
		"watch-sass": "npx sass --watch src/styles/:dist/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Identityofsine/WhitelistThem.git"
  },
  "keywords": [
    "chrome"
  ],
  "author": "IdentityofSine",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Identityofsine/WhitelistThem/issues"
  },
  "homepage": "https://github.com/Identityofsine/WhitelistThem#readme",
  "devDependencies": {
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "@types/chrome": "^0.0.263",
    "sass": "^1.71.1"
  }
}
